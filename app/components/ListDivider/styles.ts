import {StyleSheet} from 'react-native';
import {themes} from '../../config/themes';

export const styles = StyleSheet.create({
  divider: {
    width: '100%',
    height: 0.5,
    backgroundColor: themes.colors.danube[100],
  },
});
