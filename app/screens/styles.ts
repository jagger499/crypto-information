import {StyleSheet} from 'react-native';
import {themes} from '../config/themes';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: themes.colors.danube[950],
  },
  loading: {
    height: themes.sizes.VIEW_HEIGHT,
  },
});
